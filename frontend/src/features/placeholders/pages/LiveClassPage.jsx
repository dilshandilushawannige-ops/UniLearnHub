const LiveClassPage = () => {
  return (
    <section className="page-shell">
      <div className="rounded-3xl border border-slate-200 bg-white p-8 shadow-soft">
        <h1 className="text-2xl font-bold text-slate-900">Live Class</h1>
        <p className="mt-3 text-sm text-slate-600">
          Live class module UI placeholder where schedule and session join flows can be added later.
        </p>
      </div>
    </section>
  );
};

export default LiveClassPage;
